machine:
  node:
    version: 7.8.0
dependencies:
  post:
    - npm run build
deployment:
  release:
    tag: /^v(\d+\.)?(\d+\.)?(\*|\d+)$/
    commands:
      - chmod +x ./scripts/release.sh
      - ./scripts/release.sh
